# タイトル画面追加プロンプト

## 目的

新しい紀行録ギャラリーへのリンクを、トップページ (`docs/index.html`) に追加する手順を定義します。

このプロセスは、まずマスターリストである `docs/index.md` を手動で更新し、次にスクリプトを実行して `docs/index.html` を再生成するという2つのステップで構成されます。

---

## ステップ1: `index.md` に新しい紀行録のリンクを追記する

1.  `docs/index.md` ファイルを開きます。

2.  追加したい紀行録が属するカテゴリの見出しを探します。シーズナルイベントの場合は `## シーズナルイベントの紀行録` のセクションになります。

3.  そのセクションのリストの末尾に、新しい紀行録の情報を追記します。書式は、既存の項目に合わせてください。

    **例: 「紅蓮祭2025」を追加する場合**

    `## シーズナルイベントの紀行録` のセクション内にある、最後のイベント項目の下に以下の内容を追記します。

    ```markdown
    ### 紅蓮祭2025

    - [英語](./special/2025/Moonfire_Faire/index_e.html)
    - [日本語](./special/2025/Moonfire_Faire/index_j.html)
    ```

4.  ファイルを保存して閉じます。

---

## ステップ2: `index.html` の再生成

`index.md` に加えた変更をトップページに反映させるため、HTMLを再生成します。

1.  **コマンドプロンプトまたはターミナルを開きます。**

2.  **プロジェクトのルートディレクトリに移動します。**
    ```bash
    cd D:\workspace\github\ff14-unending-journey
    ```

3.  **以下のコマンドを実行して `index.html` を再生成します。**

    ```bash
    python workspace/generate_index.py
    ```

4.  **完了**:
    -   コマンドが成功すると、`docs/index.html` が更新され、追加した紀行録へのリンクがトップページに表示されます。
