# 紀行録追加プロンプト

## 目的

指定されたディレクトリと言語に基づき、そのディレクトリ内の画像フォルダから画像ギャラリーページのHTMLファイル (`index_j.html` または `index_e.html`) を自動生成するための手順を定義します。

## ワークスペースについて

プロジェクトのルートディレクトリには `workspace` というフォルダがあります。
このフォルダには、紀行録のページ生成を自動化・効率化するための補助スクリプトが格納されています。一度設定すれば、以降の作業で繰り返し利用できます。

---

## ステップ1: サムネイル画像の生成とMarkdownファイルの作成

このステップでは、指定された言語に対応する画像フォルダから、サムネイル画像と画像リストを記述したMarkdownファイルを作成します。

### 手順 (バッチファイル相当)

1.  **対象ディレクトリと画像を用意する**:
    -   `docs` ディレクトリ以下に、新しい紀行録用のディレクトリを作成します (例: `docs/special/2025/MyNewQuest`)。
    -   作成したディレクトリ内に `image_j` (日本語用) または `image_e` (英語用) フォルダを作成し、その中に画像ファイル (`.png`, `.jpg`など) を配置します。

2.  **サムネイルとMarkdownを生成する**:
    -   以下の要領で、画像からサムネイルと、画像一覧のMarkdownファイルを生成します。
    -   `TARGET_DIR`: ステップ1-1で作成したディレクトリの**絶対パス** (例: `D:\path\to\ff14-unending-journey\docs\special\2025\MyNewQuest`)
    -   `LANGUAGE`: `j` または `e`

    **a. ディレクトリ設定**:
      - 画像フォルダ: `IMG_DIR` = `TARGET_DIR\image_[LANGUAGE]`
      - サムネイルフォルダ: `THUMB_DIR` = `TARGET_DIR\image_[LANGUAGE]_thumb`
      - 出力Markdownファイル: `MD_FILE` = `TARGET_DIR\index_[LANGUAGE].md`

    **b. クリーンアップと作成**:
      - 既存の `MD_FILE` があれば削除します。
      - 既存の `THUMB_DIR` があれば削除し、再度作成します。

    **c. 画像処理**:
      - `IMG_DIR` 内の全画像ファイルに対して、ImageMagickを使用してサムネイルを生成し、Markdownファイルに追記します。
      - **推奨されるバッチファイル (`.bat`) の例:**
        このスクリプトは、サムネイルディレクトリのクリーンアップと再作成、および特殊文字のエスケープ処理を自動的に行います。

        ```batch
        @echo off
        setlocal

        set "TARGET_DIR=D:\path\to\ff14-unending-journey\docs\special\2025\MyNewQuest"
        set "LANGUAGE=j"

        set "IMG_DIR=%TARGET_DIR%\image_%LANGUAGE%"
        set "THUMB_DIR=%TARGET_DIR%\image_%LANGUAGE%_thumb"
        set "MD_FILE=%TARGET_DIR%\index_%LANGUAGE%.md"

        echo Cleaning up old files and directories...
        if exist "%MD_FILE%" del "%MD_FILE%"
        if exist "%THUMB_DIR%" rmdir /s /q "%THUMB_DIR%"
        mkdir "%THUMB_DIR%"
        echo Thumbnail directory created at: %THUMB_DIR%

        echo Generating thumbnails and markdown...
        for %%f in ("%IMG_DIR%\*" ) do (
            echo Processing %%~nxf...
            magick convert -resize 480x270 "%%f" "%THUMB_DIR%\%%~nxf.thumb.jpg"
            >>"%MD_FILE%" echo ^[![%%~nxf^](./image_%LANGUAGE%_thumb/%%~nxf.thumb.jpg^)^](./image_%LANGUAGE%/%%~nxf^)
        )

        echo Done.
        endlocal
        ```

---

## ステップ2: HTMLの自動生成

このステップでは、ステップ1で作成したMarkdownファイルと共通コンポーネントを元に、`workspace` 内のPythonスクリプトを使って最終的なHTMLページを生成します。

### 手順

1.  **コマンドプロンプトまたはターミナルを開きます。**

2.  **プロジェクトのルートディレクトリに移動します。**
    ```bash
    cd D:\workspace\github\ff14-unending-journey
    ```

3.  **以下のコマンドを実行してHTMLを生成します。**
    -   `--target_dir` には、`docs` から始まる**相対パス**を指定します。
    -   `--language` には、対象の言語 (`j` または `e`) を指定します。
    -   `--title` には、ページのタイトルを文字列で指定します（任意）。指定しない場合は、ディレクトリ名から自動生成されます。

    **基本的なコマンド:**
    ```bash
    python workspace/generate_gallery.py --project_root . --target_dir "docs/special/2025/Moonfire_Faire" --language "j"
    ```

    **タイトルを指定する場合:**
    ```bash
    python workspace/generate_gallery.py --project_root . --target_dir "docs/special/2025/Moonfire_Faire" --language "e" --title "Moonfire Faire 2025"
    ```

4.  **完了**:
    -   コマンドが成功すると、対象ディレクトリ (例: `docs/special/2025/Moonfire_Faire`) の中に `index_j.html` または `index_e.html` が生成・更新されます。

---

## トラブルシューティングと注意事項

### バッチファイルの実行
- **パスの指定:** 上記のバッチファイルの例にある `TARGET_DIR` のパスを、実際の作業対象ディレクトリの**絶対パス**に書き換えてから実行してください。
- **ImageMagick:** `magick` コマンドが動作するためには、[ImageMagick](https://imagemagick.org/) がシステムにインストールされ、環境変数 `PATH` に追加されている必要があります。

### コマンドプロンプトでの注意点
- **`for` ループの変数:** バッチファイル内では `%%f` のように `%%` を付けてループ変数を定義しますが、コマンドプロンプトから直接 `for` ループを実行する場合は `%f` のように `%` を1つだけ使用します。
- **特殊文字のエスケープ:** コマンドプロンプトで `echo` を使って `()` や `[]` などの記号を含む文字列をファイルに書き出すと、エラーが発生することがあります。これは、コマンドプロンプトがそれらの記号を特別な意味を持つ命令として解釈しようとするためです。バッチファイル内で `^` を使って `echo ^(Hello World^)` のようにエスケープするか、上記のバッチファイルの例のように記述することで、この問題を回避できます。
